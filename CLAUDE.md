# ガイドライン

## このリポジトリ

- Certus-NX Versa Evaluation Board で、SatCat5 のデモと制御のデモを行うためのリポジトリ。
- 搭載 FPGA は LFD2NX-40-8BG256C である。
- 開発は yosys を中心としたオープンソースのツールチェーンで行い、Lattice Radiant には依存しない。

## 入力とする情報

- ボードと FPGA の仕様は `datasheets/*.md` を根拠とし、内容に齟齬がないようにする。
- ピン配置、I/O 規格、ジャンパ設定、クロック源は `datasheets/*.md` を検索して確認する。
  記憶や類似ボードからの推測で書かない。
- `datasheets/*.md` は PDF から生成したもので、手で編集しない。
  PDF はリポジトリに置かず、Markdown だけをコミットする。
- 回路図と BOM のページは、図中の文字が崩れて変換されている。
  読み取れない場合は PDF の該当ページを確認する。
- データシートの追加や更新は `add-datasheet` スキルの手順に従う。

## リポジトリ構成

- リポジトリ外のローカルファイルに依存しない。
  SatCat5 などの外部 HDL は、`external/` に git submodule として置く。
- プロジェクトは `projects/<name>/` に置き、HDL、制約、パッチ、Makefile などのファイルは全てその中に置く。
  トップディレクトリに HDL 用のフォルダを作らない。
- 合成から配置配線までの手順は、プロジェクトの Makefile にまとめる。
- `tools/` の Python スクリプトは PEP 723 のインラインメタデータで依存を宣言し、`uv run` だけで動くようにする。
- ビルド生成物は `build/` に出し、コミットしない。

## ツールチェーン

- ツールは OSS CAD Suite でそろえる。
- 合成は yosys の `synth_nexus` を使う。
- SatCat5 は VHDL で書かれているため、ghdl-yosys-plugin を使って yosys に読み込む。
- 配置配線は nextpnr-nexus、ビットストリーム生成は prjoxide を使う。
- 書き込みは openFPGALoader を使い、ボード指定は `certusnx_versa_evn` とする。
- 新しいプロジェクトの Makefile は `projects/rmii_switch/Makefile` を元に作る。
  GHDL と nextpnr-nexus で必要になる対処が入っている。

```sh
prjoxide pack build/top.fasm build/top.bit
openFPGALoader -b certusnx_versa_evn build/top.bit      # SRAM に書き込む
openFPGALoader -b certusnx_versa_evn -f build/top.bit   # SPI Flash に書き込む
```

- 制約は Radiant の LDC と同じ書式の PDC ファイルで与える。
  ピンは `ldc_set_location`、I/O 規格とプルは `ldc_set_port -iobuf` で指定する。
- nextpnr-nexus はピンを割り当てていない I/O を扱えないため、全ての I/O にピンを割り当てる。
- PDC の `create_clock` は内部のクロック網に伝わらないため、クロックの目標周波数は nextpnr-nexus の `--freq` で与える。

```tcl
ldc_set_location -site {B3} [get_ports {led[0]}]
ldc_set_port -iobuf {IO_TYPE=LVCMOS33} [get_ports {led[0]}]
```

## ボードの注意

- LFD2NX-VERSA-B-EVN は、Ethernet PHY の DP83867 が未実装のまま出荷されている。
  SGMII と RGMII を使うデモの前に、U8 と U9 が実装されているかを確認する。

## コード

### 設計

- 1 ファイルには 1 モジュールだけを書き、ファイル名とモジュール名をそろえる。
- モジュールは単一の責務にする。
- 要件に対して冗長なパターン・機能・チェックを足さない。書く前に「これは過剰でないか」を自問する。
- パラメータや汎用化は、実際に 2 か所以上で必要になってから入れる。
- 数値を直接書かず、定数に名前を付ける。分周比などはクロック周波数からの計算式で書く。
- クロックとリセットの書き方は、リポジトリ全体で 1 通りにそろえる。
- 使わなくなった信号やポート、コメントアウトしたコードは残さない。

### 命名

- 仕様書・データシートにある用語に合わせる。文書にない言葉を識別子・ファイル名・コメントに使わない。
- トップモジュールのポート名は、データシートの信号名に合わせる。
- ファイル名は英語 (ASCII) にする。日本語をファイル名に使わない。

### コメント

- 「なぜ」そうしたのかを書く。コードから自明なことは書かない。
- ファイル冒頭の長いドキュメントコメントは不要。
- 他リポジトリへの言及 (「○○と同じ」「△△を参考」「踏襲」) を書かない。このリポジトリ自身の事実として書く。
- 参照先が更新される可能性があるため、参照文書の章番号や表番号を引用しない。事実そのものを書けばよい。

### テスト

- 各モジュールにテストベンチを書き、仕様にある動作をひとつずつ確認する。
- テストベンチは対象と同じ場所に `<module>_tb` という名前で置く。

## ドキュメント

- 必ず仕様書やデータシートのリファレンスを元に根拠を明確にする。
- 同じ情報を 2 か所に書かない。データシートにある値は書き写さず、どのファイルを見ればよいかを書く。
- 1 つの文には 1 つのことだけを書く。
- 用語をそろえる。例えば「書き込み」「コンフィグ」「プログラム」を混ぜて使わない。
- 太字を多用しない。
- 改行は句読点 (、。) の位置で行い、文や語の途中では改行しない。
  1 行が長くなることよりも、句読点で改行することを優先する。
- 細かく内容を補足しすぎない。特にかっこ書きによる補足が多いと読みづらくなるため避ける。
- 意図が伝わる完全な文で書く。「要点のみ。」のような断片ではなく、「ここでは要点のみ示す。」のように言い切る。
- デモごとの README には、何を確認するデモか、必要な機材、実行方法、期待する動作の 4 つだけを書く。

## 開発フロー

- コミットする前に、変更したデザインが合成・配置配線まで通ることを確認する。
- yosys と nextpnr-nexus の警告は残さない。理由があって許容する場合のみ、その箇所に根拠を添える。
- テストベンチは全てパスすること。
- コミットメッセージは英語で書く。
